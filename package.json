{
  "name": "spog-singlepaneofglass",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sizecheck": "node scripts/check-file-size.mjs",
    "prebuild": "node scripts/check-file-size.mjs",
    "build": "vite build",
    "deploy": "python3 deploy.py",
    "deploy:all": "python3 deploy.py --all",
    "deploy:next": "python3 deploy.py --next --all",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "mqtt": "^5.15.1",
    "puppeteer": "^25.2.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
